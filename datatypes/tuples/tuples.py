"""A tuple is a collection of Python objects that is:

 Ordered (items have a fixed position)

 Immutable (cannot be changed after creation)

 Allows duplicates

 Faster than lists (because they are immutable)"""